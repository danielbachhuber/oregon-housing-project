Refine or create an organization profile for the Oregon Housing Project.

The organization slug is: $ARGUMENTS

## Instructions

1. Read the organization template at `templates/organization.md` for formatting and style guidelines.

2. Read the two exemplar profiles to understand expected quality and depth:
   - `content/organizations/sightline-institute.md`
   - `content/organizations/oregon-housing-and-community-services.md`

3. Check if `content/organizations/<slug>.md` already exists:
   - If it exists, read it and refine/improve it: keep accurate information and citations, improve structure, add missing context, and fill gaps.
   - If it doesn't exist, create a new profile from scratch.

4. Use WebSearch to research this organization's role in Oregon housing policy. Search for:
   - The organization's mission, founding, and type
   - Their policy positions on housing issues
   - Specific legislation they testified on, supported, or opposed
   - News coverage of their housing-related activities
   - Official website and publications

5. Write the profile following the template guidelines exactly, then save to `content/organizations/<slug>.md`.

6. After writing the file, run `pnpm refine-internal-links content/organizations/<slug>.md --write` to add internal links automatically.
